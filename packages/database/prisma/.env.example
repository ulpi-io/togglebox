# Prisma configuration for schema generation and migrations
# Copy this file to .env and fill in the values

# Prisma provider - determines which database to use
# Valid values: mysql, sqlite, mongodb
PRISMA_PROVIDER=mysql

# Database connection URL
# The format depends on the provider:

# MySQL example:
# PRISMA_URL="mysql://<USERNAME>:<PASSWORD>@<HOST>:3306/config_db"

# SQLite example:
# PRISMA_URL="file:./dev.db"

# MongoDB example:
# PRISMA_URL="mongodb://<USERNAME>:<PASSWORD>@<HOST>:27017/config_db"

# MongoDB Connection Pool (optional - defaults shown)
# DATABASE_MAX_POOL_SIZE=10
# DATABASE_MIN_POOL_SIZE=2
# DATABASE_MAX_IDLE_TIME_MS=60000
